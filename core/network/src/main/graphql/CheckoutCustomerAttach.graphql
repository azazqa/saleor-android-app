mutation CheckoutCustomerAttach($id: ID!, $languageCode: LanguageCodeEnum!) {
  checkoutCustomerAttach(id: $id) {
    checkout {
      ...CheckoutDetails
    }
    errors {
      field
      message
      code
    }
  }
}
