mutation CheckoutAddPromoCode($id: ID!, $promoCode: String!, $languageCode: LanguageCodeEnum!) {
  checkoutAddPromoCode(id: $id, promoCode: $promoCode) {
    checkout {
      ...CheckoutDetails
    }
    errors {
      field
      message
      code
    }
  }
}
