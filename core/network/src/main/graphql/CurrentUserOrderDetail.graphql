query CurrentUserOrderDetail($id: ID!, $languageCode: LanguageCodeEnum!) {
  me {
    orders(first: 1, where: { ids: [$id] }) {
      edges {
        node {
          ...OrderDetails
        }
      }
    }
  }
}
