query ProductFavoritesProducts(
  $channel: String!
  $ids: [ID!]!
  $languageCode: LanguageCodeEnum!
) {
  products(first: 100, channel: $channel, filter: { ids: $ids }) {
    edges {
      node {
        ...ProductListItem
      }
    }
  }
}
