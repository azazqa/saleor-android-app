mutation ProductFavoritesMerge($token: UUID!) {
  productFavoritesMerge(token: $token) {
    favorites {
      id
      product {
        id
      }
    }
    errors {
      field
      message
      code
    }
  }
}
