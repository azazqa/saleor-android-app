mutation CheckoutLinesDelete(
  $id: ID!
  $lineIds: [ID!]!
  $languageCode: LanguageCodeEnum!
) {
  checkoutLinesDelete(id: $id, linesIds: $lineIds) {
    checkout {
      ...CheckoutDetails
    }
    errors {
      field
      message
      code
    }
  }
}
